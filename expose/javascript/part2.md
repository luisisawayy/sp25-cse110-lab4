1. The console would print 3 because the loop above iterates until i becomes 3 and the loop exits.
2. Line 13 will print the value inside discountedPrice, which with the numbers given will be 150.
3. Line 14 will print the value inside finalvalue variable, which in this case will also be 150.
4. The function will return the discounted prices of all the inputted numbers in an array. In this case it will return [50, 100, 150].
5. The line will return an error because i was declared with let.
6. The line will return an error because discountedprice was declared with let.
7. The value inside finalPrice will print and since it is declared within the function no error is thrown. The value printed is 150.
8. The function will return the discountPrices of all the inputted numbers in an array. IN this case, [50, 100, 150]
9. THe line will return an error because it is attempting to print i, but i is declared with let inside a loop.
10. The line will print the length of the array which is 3 in this case.
11. The function returns an array of the discounted values for all of the numbers inputted.
**12**
A. student.name
B. student["Grad Year"]
C. student.greeting()
D. student["Favorite Teacher"].name
E. student.courseLoad[0]

**13**
A. Result is 32 because the + operator triggers a string concatenation when any operand is a string.
B. Result is 1 because the - operator forces a numerical conversion to do the substraction.
C. Result is 3 because null is simply just converted to 0
D. Result is 3null because the + operator triggers string concatenation and null becomes a string
E. Result is 4 because the true becomes 1 which results in a numerical operation
F. Result is 0 since both false and null are converted to 0
G. Result is 3undefined because the + operator causes a string concatenation and undefined turns into a string.
H. Result is NaN because undefined cannot be converted to a number, even though the - operator attempts to force a numerical operation

**14**
A. Result is true since the string '2' is converted to an actual 2.
B. Result is false since both are strings and lexicographic comparison occurs
C. Result is true since the == allows coercion and thus '2' becomes an actual 2
D. Result is false because the === does not allow coercion, and the number 2 is not the same as the string '2'
E. Result is false because true becomes 1 and 1 does not equal to 2.
F. Result is true because both sides are the same type and value.

**15**
- == is the loose equality operator, compares values after type coercion if types differ
- === is the strict equality operator, it compares both type and value without conversion or coercion

17. The output is [2, 4, 6], inside the function a for loop iterates through each element of 1,2,3 and calls the doSomething function on each of them, which turns them into 2,4,6 and pushes them into the new array.

18. Output: 
1  
4  
3  
2

